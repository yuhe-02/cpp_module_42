#pragma once

#include "Base.hpp"
class B : public Base {
private:
public:
  ~B();
};

// std::ostream& operator<<(std::ostream& os, const B& B);
// std::ostream& operator<<(std::ostream& os, const B* B);
