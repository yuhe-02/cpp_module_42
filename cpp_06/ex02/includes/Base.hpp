#pragma once

// #include <string>
// #include <iostream>

class Base
{
private:

public:
	virtual ~Base();
};

// std::ostream& operator<<(std::ostream& os, const Base& Base);
// std::ostream& operator<<(std::ostream& os, const Base* Base);
